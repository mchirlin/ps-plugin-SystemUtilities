function.internalizeexpression.description=Replaces UUIDs with rule names within the given expression.

function.internalizeexpression.param.expression.description=Expression as a string that should be internalized